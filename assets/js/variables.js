var $window = $(window),
    $header = $('.js-header'),
    sh = $('.js-section').height(),
    ws = $window.scrollTop(),
    wh = $window.height(),
    wb = ws + wh,
    ww = $window.width(),
    px = 0,
    nbh = 0;
